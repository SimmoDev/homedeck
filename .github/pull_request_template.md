## What and why

<!-- What does this change do, and why? Link the roadmap item, ADR, or
issue it relates to if there is one. -->

## Test plan

<!-- How did you verify this? Be specific - "ran the tests" isn't
enough if the change touches UI or firmware behavior; see
DEVELOPMENT.md for the build/test commands for each target. -->

- [ ] `tests/` build and `ctest` pass locally
- [ ] `simulator/` builds and runs; verified visually if this touches UI
- [ ] `firmware/` builds (if this touches `firmware/` or `src/`)
- [ ] Docs updated if this changes an architectural decision, the
      roadmap, or anything `docs/` currently states as fact

## Anything reviewers should know

<!-- Deliberate scope cuts, known follow-ups, things you're unsure
about - see CLAUDE.md for the project's architecture/scope philosophy. -->
