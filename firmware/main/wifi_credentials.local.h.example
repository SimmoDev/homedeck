// Copy this file to wifi_credentials.local.h (gitignored - see .gitignore)
// and fill in your real network. wifi_credentials.local.h is never
// committed; only this .example template is.
//
// Throwaway bring-up only - proving ESP-Hosted/SDIO Wi-Fi connects on real
// hardware before the real SoftAP + wifi_provisioning flow (ADR-0006)
// replaces this. Deleted once that flow exists; real credentials belong in
// Core's encrypted NVS storage (ADR-0010), never compiled into firmware.

#define WIFI_BRINGUP_SSID "your-network-name"
#define WIFI_BRINGUP_PASSWORD "your-network-password"
